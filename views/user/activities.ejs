<%- include('../partials/header') %> 
<!--NAVIGATION BAR FOR USER-->

<%- include('../partials/userNav') %> 
<!--BOOK DETAILS-->


<header id="main-header" class="py-2 bg-primary text-white">
  <div class="container">
    <div class="row">
      <div class="col-md-6">
        <h1><i class="fa fa-pencil"></i> User Activities</h1>
      </div>
    </div>
  </div>
</header>

<!-- ACTIONS -->
<section id="actions" class="py-4 mb-4">
  <div class="container">
    <div class="row">
      <div class="col-md-3 mr-auto">
          <a href="/user/1" class="btn btn-light btn-block">
          <i class="fa fa-arrow-left"></i> Back To Dashboard
        </a>
      </div>
    </div>
  </div>
</section>


<!-- Activities -->
<section id="posts">
  <div class="container">
    <div class="row">
      <div class="col">
        <div class="card">
          <div class="card-header text-center">
            <h4>Recent Activities</h4>
          </div>
          <table class="table table-striped">
            <thead class="thead-inverse">
              <tr>
                <th>Info</th>
                <th>Category</th>
                <th>Date Posted</th>
              </tr>
            </thead>
            <tbody>
              <%for(let i = 0; i < activities.length; i++) { %>
 
                  <% if(activities[i].category =="Comment") { %>
                      <td>
                         <%=activities[i].user_id.username || 'This user'%> commented on <%=activities[i].info.title%>
                      </td>
                      <td>Comment
                        <a href="/books/details/<%=activities[i].info.id%>" class="btn btn-sm btn-secondary">
                          <i class="fa fa-angle-double-right"></i> Details
                        </a>
                      </td>
                      
                      <td><%=activities[i].entryTime.toDateString()%></td>
                  <% } else if(activities[i].category =="Update Comment") { %>
                      <td>

                         <%=activities[i].user_id.username || 'This user'%> updated comment on <%=activities[i].info.title%>
                      </td>
                      <td>Update Comment</td>
                      <td><%=activities[i].entryTime.toDateString()%></td>
                  <% } else if(activities[i].category =="Delete Comment") { %>
                      <td>
                         <%=activities[i].user_id.username || 'This user'%> deleted comment on <%=activities[i].info.title%>
                      </td>
                      <td>Delete Comment</td>
                      <td><%=activities[i].entryTime.toDateString()%></td>
                  <% } %>
              </tr>
                
              <%  } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</section>



<%- include('../partials/footer') %> 